<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>url shortner</title>
</head>

<body>
    <h1>url shortner</h1>

    <% if (locals.error) { %>
        <p class="errors">
            <%= error %>
        </p>

        <% } %>
            <form action='/' method='post'>
                <input type="url" name="url" id="url" placeholder="Give you Url">
                <input type="submit" value="Submit url">
            </form>

            <div class="short-url">
                <% if (locals.short_url) { %>
                    <a href="<%= short_url %>">
                        <%= short_url %>
                    </a>
                    <% } %>
            </div>

</body>

</html>